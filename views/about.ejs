<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>About Us - College CMS</title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
  <style>
    .about-hero {
      background: linear-gradient(135deg, rgba(14, 165, 233, 0.9), rgba(2, 132, 199, 0.8)), url('/css/images/sherubtse.jpg') center/cover no-repeat;
      color: var(--text-inverse);
      padding: var(--space-20) 0;
      border-bottom: 1px solid var(--border-light);
      position: relative;
      overflow: hidden;
    }
    .about-hero::before {
      content: '';
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: radial-gradient(circle at 20% 80%, rgba(14, 165, 233, 0.3) 0%, transparent 50%);
      pointer-events: none;
    }
    .about-hero .container { position: relative; z-index: 1; }
    .about-hero h1 { 
      font-family: 'Poppins', sans-serif;
      font-size: var(--text-5xl); 
      font-weight: var(--font-black);
      margin-bottom: var(--space-4); 
      background: linear-gradient(135deg, #ffffff, #e2e8f0);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    .about-hero p { color: rgba(255, 255, 255, 0.9); font-size: var(--text-lg); }
    .about-section { padding: var(--space-16) 0; background: var(--bg-primary); }
    .grid-2 { display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr)); gap: var(--space-8); }
    .about-card { 
      background: var(--bg-primary); 
      border: 1px solid var(--border-light); 
      border-radius: var(--radius-2xl); 
      padding: var(--space-8);
      box-shadow: var(--shadow-sm);
      transition: all var(--transition-normal);
    }
    .about-card:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-md);
      border-color: var(--primary-200);
    }
    .about-card h3 { 
      font-family: 'Poppins', sans-serif;
      color: var(--primary-600); 
      margin-top: 0;
      font-size: var(--text-2xl);
      font-weight: var(--font-bold);
      margin-bottom: var(--space-4);
    }
    .muted { color: var(--text-secondary); }
    .value-list { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: var(--space-6); margin-top: var(--space-8); }
    .value { 
      background: linear-gradient(135deg, var(--primary-50), var(--primary-100)); 
      padding: var(--space-6); 
      border-radius: var(--radius-xl); 
      border: 1px solid var(--primary-200);
      transition: all var(--transition-normal);
    }
    .value:hover {
      transform: translateY(-2px);
      box-shadow: var(--shadow-md);
      border-color: var(--primary-300);
    }
    .value strong {
      font-family: 'Poppins', sans-serif;
      color: var(--primary-700);
      font-weight: var(--font-bold);
      font-size: var(--text-lg);
      display: block;
      margin-bottom: var(--space-2);
    }
    .team { padding: var(--space-16) 0; background: var(--bg-secondary); }
    .team-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(220px, 1fr)); gap: var(--space-6); }
    .member { 
      background: var(--bg-primary); 
      border: 1px solid var(--border-light); 
      border-radius: var(--radius-2xl); 
      padding: var(--space-6); 
      text-align: center;
      transition: all var(--transition-normal);
      box-shadow: var(--shadow-sm);
    }
    .member:hover {
      transform: translateY(-4px);
      box-shadow: var(--shadow-md);
      border-color: var(--primary-200);
    }
    .member h4 { 
      font-family: 'Poppins', sans-serif;
      margin: var(--space-3) 0 var(--space-2); 
      color: var(--text-primary);
      font-weight: var(--font-bold);
      font-size: var(--text-lg);
    }
    .breadcrumbs { font-size: var(--text-sm); color: rgba(255, 255, 255, 0.8); margin-bottom: var(--space-4); }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar" id="navbar">
    <div class="container nav-container">
      <a href="/" class="logo">
        <img src="/css/images/logo.jpg" alt="College FMS Logo">
        <span>College FMS</span>
      </a>
      <ul class="nav-links">
        <li><a href="/home">Home</a></li>
        <li><a href="/about">About</a></li>
        <li><a href="/redressal">Redressal</a></li>
        <li><a href="/feedback">Feedback</a></li>
        <li><a href="/contact">Contact</a></li>
        <% if (currentUser && currentUser.role === 'admin') { %>
          <li><a href="/admin" class="btn btn-sm btn-primary">Admin Panel</a></li>
        <% } %>
        <% if (currentUser) { %>
          <li><a href="/logout" class="btn btn-sm btn-ghost">Logout</a></li>
        <% } else { %>
          <li><a href="/login" class="btn btn-primary">Login</a></li>
          <li><a href="/signup" class="btn btn-secondary">Sign Up</a></li>
        <% } %>
      </ul>
    </div>
  </nav>

  <!-- About Hero -->
  <section class="about-hero">
    <div class="container">
      <div class="breadcrumbs"></div>
      <h1>About the College Management & Redressal System</h1>
      <p>Our mission is to simplify academic workflows and ensure transparent redressal.</p>
    </div>
  </section>

  <!-- About Content -->
  <section class="about-section">
    <div class="container grid-2">
      <div class="about-card">
        <h3>Our Mission</h3>
        <p class="muted">We empower students, faculty, and administrators with a unified platform to manage academics and address grievances efficiently, fairly, and transparently.</p>
      </div>
      <div class="about-card">
        <h3>What We Offer</h3>
        <ul class="muted">
          <li>Centralized academic records and announcements</li>
          <li>Complaint submission, tracking, and resolution</li>
          <li>Role-based, secure access for all stakeholders</li>
          <li>Modern, responsive UI for an accessible experience</li>
        </ul>
      </div>
    </div>
  </section>

  <section class="about-section">
    <div class="container">
      <h2 class="section-title">Our Values</h2>
      <div class="value-list">
        <div class="value"><strong>Transparency</strong><br><span class="muted">Clear status and timelines on every complaint.</span></div>
        <div class="value"><strong>Reliability</strong><br><span class="muted">Stable and secure infrastructure for daily use.</span></div>
        <div class="value"><strong>Inclusivity</strong><br><span class="muted">Accessible design for all users and devices.</span></div>
        <div class="value"><strong>Accountability</strong><br><span class="muted">Auditable actions and fair outcomes.</span></div>
      </div>
    </div>
  </section>

  <section class="team">
    <div class="container">
      <h2 class="section-title">Meet the Team</h2>
      <div class="team-grid">
        <div class="member">
          <h4>Administrator</h4>
          <p class="muted">Oversees operations and ensures policy compliance.</p>
        </div>
        <div class="member">
          <h4>Faculty Coordinator</h4>
          <p class="muted">Monitors academic modules and student progress.</p>
        </div>
        <div class="member">
          <h4>Redressal Officer</h4>
          <p class="muted">Ensures timely and fair complaint resolutions.</p>
        </div>
        <div class="member">
          <h4>Student Representative</h4>
          <p class="muted">Advocates for student needs and accessibility.</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Footer -->
  <footer>
    <div class="container">
      <div class="footer-content">
        <div class="footer-brand">
          <a href="/" class="footer-logo">
            <img src="/css/images/logo.jpg" alt="College CMS Logo">
            <span>College CMS</span>
          </a>
          <p class="footer-description">
            Empowering educational institutions with modern technology solutions for seamless academic management and student success.
          </p>
        </div>
        <div class="footer-section">
          <h4>Platform</h4>
          <ul class="footer-links">
            <li><a href="/about">About</a></li>
            <li><a href="/redressal">Redressal</a></li>
            <li><a href="/feedback">Feedback</a></li>
            <li><a href="/contact">Contact</a></li>
          </ul>
        </div>
        <div class="footer-section">
          <h4>Legal</h4>
          <ul class="footer-links">
            <li><a href="/privacy">Privacy Policy</a></li>
            <li><a href="/terms">Terms</a></li>
          </ul>
        </div>
      </div>
      <div class="footer-bottom">
        <p>&copy; 2025 College Facilities & Management System. All rights reserved. Built for education.</p>
      </div>
    </div>
  </footer>

  <script>
    window.addEventListener('scroll', () => {
      const navbar = document.getElementById('navbar');
      if (window.scrollY > 50) {
        navbar.classList.add('scrolled');
      } else {
        navbar.classList.remove('scrolled');
      }
    });
    (function(){
      const path = window.location.pathname;
      document.querySelectorAll('.nav-links a').forEach(a => {
        if (a.getAttribute('href') === path) {
          a.classList.add('active');
        }
      });
    })();
  </script>
</body>
</html>

